{% extends 'base.html.twig' %}


{% block body %}
    <div class="container my-5">
        <h1> Ma commande </h1>
        <div class="row">
            <div class="col-md-3">
                {{ form_start(deliveryForm) }}
                    {{ form_row(deliveryForm.addresses) }}
                <a href="{{ path('app_account_address_form') }}" class="small text-decoration-none"> Ajouter une nouvelle adresse</a>                    {{ form_row(deliveryForm.carriers) }}
                {{ form_end(deliveryForm) }}
            </div>
        </div>
    </div>

{% endblock %}
